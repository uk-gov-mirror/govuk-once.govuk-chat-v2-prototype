name: Agent Dev CI

on:
  workflow_dispatch: {}
  push:
    branches:
      - main
    paths:
      - '.github/actions/setup-python-uv-env/*'
      - '.github/actions/setup-node-pnpm-env/*'
      - '.github/workflows/agent-dev-ci.yaml'
      - 'uv.lock'
      - 'pnpm-lock.yaml'
      - 'scripts/agent-dev.sh'
      - 'utilities/agent-dev/**'
  pull_request:
    paths:
      - '.github/actions/setup-python-uv-env/*'
      - '.github/actions/setup-node-pnpm-env/*'
      - '.github/workflows/agent-dev-ci.yaml'
      - 'uv.lock'
      - 'pnpm-lock.yaml'
      - 'scripts/agent-dev.sh'
      - 'utilities/agent-dev/**'

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: ./.github/actions/setup-python-uv-env
        with:
          dependencies_github_token: ${{ secrets.PRIVATE_REPO_ACCESS_GITHUB_TOKEN }}

      - name: Run tests
        run: utilities/agent-dev/scripts/test.sh

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: ./.github/actions/setup-python-uv-env
        with:
          dependencies_github_token: ${{ secrets.PRIVATE_REPO_ACCESS_GITHUB_TOKEN }}

      - name: Run linter
        run: utilities/agent-dev/scripts/lint.sh

  ruff_format:
    name: Ruff formatter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: ./.github/actions/setup-python-uv-env
        with:
          dependencies_github_token: ${{ secrets.PRIVATE_REPO_ACCESS_GITHUB_TOKEN }}

      - name: Run formatter
        run: utilities/agent-dev/scripts/format-ruff.sh

  prettier_format:
    name: Prettier formatter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: ./.github/actions/setup-node-pnpm-env

      - name: Run Prettier
        run: utilities/agent-dev/scripts/format-prettier.sh

  typing:
    name: Type checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: ./.github/actions/setup-python-uv-env
        with:
          dependencies_github_token: ${{ secrets.PRIVATE_REPO_ACCESS_GITHUB_TOKEN }}

      - name: Run type checks
        run: utilities/agent-dev/scripts/type-check.sh
